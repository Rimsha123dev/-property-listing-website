
{
  "version": 2,
  "builds": [
    { "src": "backend/api/**/*", "use": "@vercel/node" },
    { "src": "frontend/next.config.ts", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/api/$1" },
    { "src": "/(.*)", "dest": "frontend/$1" }
  ]
}
